# PagedOut — Requirements
# Python 3.11+
#
# These are the versions the code is actually developed and run against.
# Everything below is imported somewhere in the codebase.

# Agent orchestration
langgraph==1.1.6
langchain-core==1.2.29
langchain-ollama==1.1.0

# Vector search + embeddings
qdrant-client==1.17.1
sentence-transformers==3.3.1

# Kafka producer (pipeline/log_generator.py)
kafka-python==2.3.1

# Dataset scraper (finetuning/dataset/scraper/)
requests==2.31.0
beautifulsoup4==4.12.2

# Shared
numpy==1.26.4


# ── Not yet used ──────────────────────────────────────────────────────────────
# Kept here as a record of intent, deliberately not installed. Each of these
# belongs to a roadmap item that has no code yet — see "Not built yet" in the
# README. Uncomment when the corresponding feature actually lands.
#
# Fine-tuning (no training script exists yet)
#   transformers, peft, trl, bitsandbytes, datasets, accelerate
#
# Evaluation (no eval harness exists yet)
#   ragas
#
# Observability (nothing is instrumented yet)
#   langsmith, arize-phoenix, mlflow, opentelemetry-api, opentelemetry-sdk
#
# API + frontend (api/ and frontend/ are empty)
#   fastapi, uvicorn, celery
#
# Graph checkpointing (graph.compile() takes no checkpointer yet)
#   psycopg2-binary, sqlalchemy, redis, langgraph-checkpoint-postgres
#
# Output validation (risk check is currently a string prefix)
#   guardrails-ai, pydantic
